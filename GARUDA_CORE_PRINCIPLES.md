# GARUDA CORE PRINCIPLES — Founder-Approved Product Constitution (ENFORCEABLE)

> **Supreme:** GARUDA MUST MAKE COMPLEXITY SIMPLE. Being complex is not GARUDA. Making complexity easy and simple is GARUDA.

This document describes **reality, not promises**. Every principle has `POLICY → CONSUMER → RUNTIME EFFECT → TEST EVIDENCE`. If test missing, marked `DOCUMENTED ONLY`.

Machine-readable source: `src/services/garudaCorePrinciples.js` (`GARUDA_CORE_PRINCIPLES`, `getQualityFloor()`).

## PRINCIPLE MAP

| # | Principle | Meaning (Founder) | Enforcement Point | Code Consumer | Runtime Effect | Test Evidence |
|---|---|---|---|---|---|---|
| 1 | **Simplicity** | Hide APIs/models/providers/workflows unless user asks advanced control. Simple input, powerful output. | `CreativeStudioService.createCreativeBrief` defaults (`channel→meta_instagram`, `audience` default) and `Mother understandGoal` keyword intent | `creativeStudioService.js:105`, `scripts/mother/goalEngine.js`, `garudaCorePrinciples.js:simplicity` | User can say “Ek cinematic video bana do” without provider/model params; system picks internally | `garudaCorePrinciples.test.js: simple brief without technical params → BRIEF_CREATED` |
| 2 | **Natural Language First** | Human language primary: `USER INTENT→UNDERSTANDING→PLANNING→ORCHESTRATION→QUALITY→OUTPUT` | `Mother/Mother.js` + `garudaCommandRouter` intent classification | `scripts/mother/mother.js`, `garudaCorePrinciples.js:natural_language` | Hindi/English mix mapped to `createCreativeBrief` without exposing codec/resolution | `garudaCorePrinciples.test.js: Hindi intent → brief created` |
| 3 | **Quality Non-Negotiable** | **BEYOND_EXPECTATION_QUALITY** — “19 nahi, 22” is philosophy (exceed expectation), NEVER numeric 22/98. Distinguish `TECHNICAL_VERIFICATION` (file/MIME/dims/SHA), `REQUIREMENT_COMPLIANCE` (format/dims/CTA/identity), `VISUAL_QUALITY` (only VERIFIED if semantic model exists, else `VISUAL_QUALITY_NOT_YET_VERIFIED`). PREVIEW vs DELIVERY distinct. | `creativeQualityService.validateAsset` (separates 3 verifications) + `garudaCorePrinciples.js:quality` | `creativeQualityService.js: technicalVerification + requirementCompliance + visualQualityVerification`, `imageGenerationRouter.js` | Technical + requirement must pass; visual remains `NOT_YET_VERIFIED` truthfully; no fake 98 | `garudaCorePrinciples.test.js: physical ≠ visual, visual not yet verified` |
| 4 | **Brand Identity Never Drifts** | Identity Lock for face/voice/logo/style across scenes; provider switch must not switch identity | `identityLockService` + router `brandId/projectId/identityId/styleProfileId/continuityRequired` propagation | `identityLockService.js`, `imageGenerationRouter.js:brand continuity`, `videoGenerationRouter.js`, `creativeStudioService.js` | Every `routeGeneration` carries `brandId/projectId/identityId` → `lockHash` validated in `validateAsset` → `IDENTITY_LOCK_HASH_VERIFIED` | `garudaCorePrinciples.test.js: brand switch preserves lockHash check` |
| 5 | **Provider-Independent, Quality-Dependent** | Provider is tool, GARUDA is product. User sees GARUDA not Gemini/Runway. Optimize quality>consistency>reliability>cost>sovereignty. Cost never overrides floor. | `imageGenerationRouter`, `videoGenerationRouter`, `audioGenerationRouter`, `smartModelRouter` capability matching | `imageGenerationRouter.js:detectProviders` (no user provider param), `garudaCorePrinciples.js:provider_independence` | No `request.provider` from user accepted; selection internal `activeAIProviders[0]` + fallback; cost not override floor | `garudaCorePrinciples.test.js: provider abstraction` |
| 6 | **Free First, Sovereign Always** | External → Adapter → Garuda Capability Interface → Quality/Governance → Output. Replaceable for in-house. | All routers adapter pattern + sovereign SVG/storyboard `alwaysAvailable` | `imageGenerationRouter.js:garuda_sovereign_svg_renderer`, `videoGenerationRouter.js:garuda_storyboard_engine` | With zero keys, `sovereignSvgAvailable:true` → `VECTOR_CREATIVE` with SHA-256 verified file | `creativeMarketingHostileVerification.test.js: sovereign SVG` |
| 7 | **OS not Toolbox** | One intelligence coordinates Creative Understanding→Story→Script→Storyboard→Visual→Voice→Music→Assembly→Review | `CreativeStudioService` orchestration (brief → concept → family → assets → storyboard → campaign) | `creativeStudioService.js: createBrief→generateConcept→generateCampaignFamily` | `Ek video bana` triggers `orchestrateProjectGrowthCampaign` not manual 10-tool chain | Existing hostile test `orchestrateProjectGrowthCampaign` |
| 8 | **Autonomy with Governance** | Creative autonomous; financial/deployment/permanent engineering require founder approval, worktree, review, no auto commit/push | `EngineeringPipeline`, `DevelopmentApprovalGate`, `ApprovalGate`, `executor.js` | `engineeringPipeline.js`, `missionControlService.js`, `garudaCorePrinciples.js:governance` | `founderApproved:false` + direct → `BLOCKED`; worktree allowed; `git status` no commits | `engineeringPipeline.test.js` governance (Phase B) |
| 9 | **Intelligence Invisible** | More intelligence → less user complexity. Expose Create/Build/Generate not provider params. | All user-facing routes hide `qualityProfile`, `provider` | `CreativeStudioService`, `garudaCorePrinciples.js:intelligence_invisible` | Defaults handle complexity; advanced `qualityProfile` optional | `garudaCorePrinciples.test.js: simple input → professional output` |
| 10 | **One Request → Complete Workflow** | One intention → multi-step → quality validation → finished result. Never fake completion. | `CreativeStudioService.generateCampaignFamily` (7 assets + storyboard) + `EngineeringPipeline` truth laws | `creativeStudioService.js`, `growthSharedContracts.js: PROVIDER_UNAVAILABLE !== READY` | If provider unavailable → `PROVIDER_UNAVAILABLE` truth, not fake `GENERATED`; delivers promptPackage + vector fallback | `hostileVerification: truthful unavailable` |
| 11 | **Creative Universe Unified** | TEXT/IMAGE/VIDEO/VOICE/MUSIC/PPT/PDF unified via `CREATIVE DIRECTOR → SPECIALIST ENGINES → QUALITY+CONSISTENCY` | `CreativeStudioService` + routers + `audioGenerationRouter` | `creativeStudioService.js`, `audioGenerationRouter.js`, `garudaCorePrinciples.js:creative_universe` | Single `briefId` drives image+video+audio via same `brandId/projectId` | `garudaCorePrinciples.test.js: unified director` |
| 12 | **Brand-Critical Cinematic Mode** | `cinematic/flagship/brand film` requires highest consistency, high-res, continuity, review. `Ek video bana` ≠ `flagship cinematic`. This is BEYOND_EXPECTATION ambition, not numeric 98. | `getQualityFloor("cinematic") → exceptional_completeness` + `continuityRequired` | `creativeStudioService.js: title.includes(cinematic)→cinematic`, `videoGenerationRouter.js: qualityProfile`, `creativeQualityService.js` | Cinematic brief gets `exceptional_completeness` requirement, standard `full_completeness`; visual still `NOT_YET_VERIFIED` until model | `garudaCorePrinciples.test.js: cinematic vs standard level` |
| 13 | **Failure Not User Burden** | Provider failure → retry → alternative provider → alternative strategy → partial recovery before surfacing | `imageGenerationRouter` catch→ sovereign fallback, `EngineeringPipeline` retry loop | `imageGenerationRouter.js: catch→fallback`, `videoGenerationRouter.js`, `engineeringPipeline.js: retry` | `executeAIImageProvider` failure returns `fallbackAsset` with `fallbackUsed:true` | `garudaCorePrinciples.test.js: provider failure → fallback` |
| 14 | **Memory Improves Experience** | Learn from failures, successes, brand preferences; next similar task better | `persistentMemory`, `outcomeLearningService`, `smartModelRouter learningContext` | `garudaCorePrinciples.js:memory`, `smartModelRouter.js`, `engineeringPipeline.js` Phase C | `learningContext` influences `smartRouter` provider switch after 2 failures | Phase C A/B proof |
| 15 | **Product Language** | Human, simple, clear: “Bhai, cinematic version ready kar raha hoon.” Adapt to user Hindi/English | `garudaCommandRouter`, `llmProvider`, `CreativeStudioService` responses | `garudaCorePrinciples.js:product_language` | Responses adapt, not technical orchestration logs | `DOCUMENTED ONLY` (no runtime enforcement yet beyond prompt) |
| 16 | **Living Artifact** | GARUDA stands behind what it creates. Presentation/report/proposal retains `purpose/audience/narrative/keyClaims/evidence/assumptions/decisions/risks/anticipatedQuestions/conversationContext` to CREATE→PRESENT→EXPLAIN→ANSWER→CONTINUE | `livingArtifactService` (create/get/preparePresentation/answer) + `persistentMemory` | `livingArtifactService.js` reused persistentMemory, Mother, RAG | Investor deck “What is GARUDA” creates living context with narrative + 10 anticipated questions (PRODUCT/MOAT/REVENUE etc.), can present conversationally and answer `EVIDENCE_BACKED/REASONED/ASSUMPTION/UNKNOWN` without fabrication | `livingArtifactService.test.js: F-L (12 tests)` |
| 17 | **Dynamic Visual Outreach & Identity Truth** | Golden Rule: All outreach emails & proposals must be visual-first, dynamically styled to client's industry/brand ("Rang, Roop & Mood"), zero cross-brand pollution (no Niravi leakage), and zero fake phone numbers or fake emails. Only verified Founder channels. | `premiumVisualEmailService`, `garudaOutreachDispatchService`, `emailRelayService` | `premiumVisualEmailService.js: dynamicThemes + verifiedIdentity`, `emailRelayService.js: htmlContent` | Emails generate responsive HTML with industry-specific palettes; fake phone numbers strictly blocked; SHA-256 evidence on disk | `premiumVisualEmailService.test.js: dynamic theme + zero fake contact` |

**Final 10-Question Gate** (src/services/garudaCorePrinciples.js:finalTest10) must be asked per feature: simpler? hides complexity? preserves quality/consistency? replaceable provider? sovereignty? performs work? avoids fake? preserves governance? non-technical usable? If NO → reconsider.

### Enforcement Summary
- **Policy file:** `src/services/garudaCorePrinciples.js` (BEYOND_EXPECTATION, technical/requirement/visual separation) consumed by `creativeQualityService`, `imageGenerationRouter`, `videoGenerationRouter`, `audioGenerationRouter`, `creativeStudioService`, `livingArtifactService`.
- **Runtime effect proven:** BEYOND_EXPECTATION ambition (not numeric 98), technical vs visual truth separation, provider abstraction, brand continuity, resilience fallback, natural language, OS orchestration, living artifact continuity — each has test evidence below.
- **Documented only:** Product language tone beyond prompt adaptation; full semantic visual scoring.

### Test Evidence Index
- `src/services/garudaCorePrinciples.test.js` (Phase 2.1 corrected): BEYOND_EXPECTATION not numeric, physical≠visual, technical vs visual separated.
- `src/services/creativeGoldenPath.test.js`: golden path, DRY_RUN SIMULATED_GENERATION, LIVE blocked, identity propagation, fallback, no secret leakage, truth model distinction.
- `src/services/livingArtifactService.test.js`: A-L (12 tests: 19 vs 22 not numeric, technical objective, physical≠visual, living create/retrieve/present/answer, investor example, unknown not fabricated).
- `src/services/creativeMarketingHostileVerification.test.js`: sovereign fallback, 6-point verification, truth laws.
- `src/services/engineeringPipeline/engineeringPipeline.test.js`: governance, no auto commit.
- Phase C A/B proof: memory → routing influence.
