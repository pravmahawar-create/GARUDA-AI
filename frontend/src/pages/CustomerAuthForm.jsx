import React, { useState } from "react";
import { Link } from "react-router-dom";
async function postEmailPassword(url, email, password) { const response = await fetch(url, { method: "POST", headers: { "Content-Type": "application/json" }, credentials: "same-origin", body: JSON.stringify({ email, password }) }); const data = await response.json(); if (!response.ok) throw new Error(data.message || "Unable to continue"); return data; }
function enterApp() { window.location.assign("/app"); }
export default function CustomerAuthForm({ mode }) { const isSignup = mode === "signup"; const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const [demoLoading, setDemoLoading] = useState(false); async function submit(event) { event.preventDefault(); setError(""); setLoading(true); try { await postEmailPassword(`/api/customer/${isSignup ? "signup" : "login"}`, email, password); enterApp(); } catch (authError) { setError(authError.message); } finally { setLoading(false); } } async function demoLogin() { setError(""); setDemoLoading(true); try { const response = await fetch("/api/customer/demo", { method: "POST", credentials: "same-origin" }); const data = await response.json(); if (!response.ok) throw new Error(data.message || "Unable to start the demo"); enterApp(); } catch (authError) { setError(authError.message); } finally { setDemoLoading(false); } } return <main className="garuda-shell" style={{ minHeight: "100vh", display: "grid", placeItems: "center" }}><form onSubmit={submit} style={{ width: "min(100% - 2rem, 400px)", padding: "2rem", border: "1px solid rgba(255,255,255,.15)", borderRadius: "12px", background: "#111827" }}><p className="eyebrow">GARUDA AI</p><h1 style={{ marginTop: 0 }}>{isSignup ? "Create your account" : "Welcome back"}</h1><label htmlFor="customer-email">Email</label><input id="customer-email" type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} required style={{ width: "100%", margin: "0.5rem 0 1rem", padding: "0.75rem" }} /><label htmlFor="customer-password">Password</label><input id="customer-password" type="password" autoComplete={isSignup ? "new-password" : "current-password"} value={password} onChange={(event) => setPassword(event.target.value)} minLength="12" required style={{ width: "100%", margin: "0.5rem 0 1rem", padding: "0.75rem" }} />{isSignup && <p style={{ color: "#9ca3af", fontSize: "0.85rem" }}>Use at least 12 characters.</p>}{error && <p role="alert" style={{ color: "#fca5a5" }}>{error}</p>}<button type="submit" disabled={loading || demoLoading} className="hero-panel__button hero-panel__button--primary" style={{ width: "100%" }}>{loading ? "Please wait..." : isSignup ? "Create account" : "Log in"}</button><p style={{ color: "#9ca3af", marginBottom: 0 }}>{isSignup ? "Already have an account?" : "New to GARUDA?"} <Link to={isSignup ? "/login" : "/signup"} style={{ color: "#fbbf24" }}>{isSignup ? "Log in" : "Get started"}</Link></p><div style={{ display: "flex", alignItems: "center", gap: "0.75rem", margin: "1.25rem 0" }}><span style={{ flex: 1, height: 1, background: "rgba(255,255,255,.12)" }} /><span style={{ color: "#6b7280", fontSize: "0.8rem" }}>or</span><span style={{ flex: 1, height: 1, background: "rgba(255,255,255,.12)" }} /></div><button type="button" disabled={demoLoading || loading} onClick={demoLogin} style={{ width: "100%", background: "transparent", border: "1px solid rgba(251,191,36,.45)", color: "#fbbf24", padding: "0.75rem", borderRadius: "8px", fontWeight: 700, cursor: "pointer", fontSize: "0.95rem" }}>{demoLoading ? "Preparing demo…" : "Try a one-click demo account"}</button></form></main>; }