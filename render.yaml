services:
  - type: web
    name: garuda-ai
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
